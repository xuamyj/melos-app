import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            Text("Settings — Coming Soon")
                .navigationTitle("Settings")
        }
        .navigationViewStyle(.stack)
    }
}
