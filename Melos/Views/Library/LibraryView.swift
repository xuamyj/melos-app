import SwiftUI

struct LibraryView: View {
    var body: some View {
        NavigationView {
            Text("Library — Coming Soon")
                .navigationTitle("Library")
        }
        .navigationViewStyle(.stack)
    }
}
